'use strict';

const url="https://api.github.com/users/"

function watchForm(){
  $('.search-form').submit(event => {
    event.preventDefault();
    $(".js-results-list").empty();
    let userSearchInput = $(".username-input").val();
    let 
  })
}

function getUsers(url){
  
}